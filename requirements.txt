# Phase A (data_prep/) needs: torch/torchvision (for VOCDetection downloader), pyyaml, matplotlib, pillow
# Phase B (training/) additionally needs: torchmetrics + pycocotools (Faster R-CNN mAP) and ultralytics (YOLOv8 + RT-DETR)
#
# On Colab, torch/torchvision/pillow/matplotlib are already preinstalled with CUDA support --
# only install the lines below to avoid accidentally overwriting the GPU build.
torchmetrics>=1.4
pycocotools>=2.0.7
ultralytics>=8.2
pyyaml>=6.0
